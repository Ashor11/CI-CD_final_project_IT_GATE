apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config-js
  namespace: {{ .Release.Namespace }}
data:
  config.js: |
    window.REACTIONS_SVC_URL = '{{ .Values.frontend.config.REACTIONS_SVC_URL }}';
    window.MOOD_SVC_URL = '{{ .Values.frontend.config.MOOD_SVC_URL }}';
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: {{ .Release.Namespace }}
data:
  REACTIONS_SVC_URL: {{ .Values.frontend.config.REACTIONS_SVC_URL | quote }}
  MOOD_SVC_URL: {{ .Values.frontend.config.MOOD_SVC_URL | quote }}
---
apiVersion: v1
kind: Secret
metadata:
  name: frontend-secret
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData: {}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: {{ .Release.Namespace }}
  labels:
    app: frontend
spec:
  replicas: {{ .Values.frontend.replicaCount }}
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: {{ .Values.frontendImage | quote }}
          ports:
            - containerPort: {{ .Values.frontend.service.port }}
          volumeMounts:
            - name: config-js
              mountPath: /usr/share/nginx/html/config.js
              subPath: config.js
      volumes:
        - name: config-js
          configMap:
            name: frontend-config-js
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: {{ .Values.frontend.service.port }}
      name: http
---
{{- if .Values.networkPolicies.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: frontend-networkpolicy
  namespace: {{ .Release.Namespace }}
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from: []
      ports:
        - protocol: TCP
          port: 80
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Release.Namespace }}
      ports:
        - protocol: TCP
          port: 8080
{{- end }}
